version: '2'
services:
  cuenta-database:
    environment:
      MYSQL_DATABASE: cuenta_dev
  cuenta-application:
    image: registry.neec.xyz/dev/cuenta-application
    volumes:
      - .:/app
    environment:
      - MIX_ENV=dev
    ports:
      - "4000:4000"
